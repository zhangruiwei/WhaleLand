{
  "Kafka": {
    "Sender": {
      "Debug": "broker,topic,msg",
      "bootstrap.servers": "localhost:9092"

    },
    "Receiver": {
      "Debug": "consumer,cgrp,topic,fetch",
      "GroupId": "dev1",
      "bootstrap.servers": "localhost:9092"
    }
  }
}
